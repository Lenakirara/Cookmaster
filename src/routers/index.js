const userRouter = require('./userRouters');
const loginRouter = require('./loginRouters');
const recipeRouter = require('./recipesRouters');

module.exports = {
  userRouter,
  loginRouter,
  recipeRouter,
};
